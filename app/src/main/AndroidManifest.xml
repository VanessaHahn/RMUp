<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mi.ur.de.android.runnersmeetup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_runnersmeetup"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".NavigationDrawer"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RMU_Main" android:parentActivityName=".NavigationDrawer"
            android:label="Run"/>
        <activity android:name=".Trainingsuebersicht" android:parentActivityName=".NavigationDrawer"
            android:label="Trainingsübersicht"/>

        <service
            android:name=".CalculatorService"
            android:enabled="true" />

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Settings" android:parentActivityName=".NavigationDrawer"
            android:label="Einstellungen"/>
    </application>

</manifest>